import {  } from 'react';

import type { Reservation } from "../types/type";


interface ReservationFormProps {
  reservation: Reservation;   // ← ONE prop, 
}// you're using it to describe the shape of your prop.

function ReservationForm({ Reservation }: ReservationFormProps) {
    return (

    )


}
